import 'package:flutter/material.dart';

class ChartDurations extends StatelessWidget {
  const ChartDurations({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder(fallbackHeight: 190);
  }
}
